interface:
  display_name: "To Tickets"
  short_description: "Split a plan into tracer-bullet tickets"
  default_prompt: "使用 $to-tickets 把当前规格拆成带依赖关系的纵向工单。"
policy:
  allow_implicit_invocation: false
